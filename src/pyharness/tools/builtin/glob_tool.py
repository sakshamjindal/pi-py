"""`glob` tool. Implementation lands at Stage 4.

The module is named ``glob_tool`` to avoid shadowing the stdlib ``glob``
module on import; the tool itself is registered under the name ``glob``.
"""
