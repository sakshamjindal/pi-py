"""pyharness — a minimal Python agent harness.

The public SDK surface lands at Stage 16. This module is intentionally
empty during early stages so that internal refactors don't churn the
public API.
"""

__version__ = "0.1.0"
