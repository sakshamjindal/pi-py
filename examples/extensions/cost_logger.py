"""Example extension: token-cost logger.

Stub during Stage 1. The full implementation — subscribing to
``after_llm_call``, accumulating token costs, writing to a file — lands at
Stage 11 once the extension API is implemented.
"""
